language: c
compiler:
  - clang
  - gcc
  
addons:
  apt:
    packages:
      - autoconf-archive
      - inttool
      - libgtk-3-dev
      - libsdl2-dev
      - libxtst-dev

script:
  - ./autogen.sh
  - make
